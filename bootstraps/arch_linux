# TODO: Make this a script for base install on arch...

# My GUI
# hyprland is from some distro

# Terminal and fonts
pacman -S wezterm 

pacman -S hyprlock

# Install some configuration backends that would enable dark mode... unfinished...
pacman -S xdg-desktop-portal-hyprland xdg-desktop-portal-gtk

# Install baseline development env
# make gcc etc
pacman -S jq yq

pacman -S github-cli
curl -fsSL https://gh.io/copilot-install | bash


# Install Rust
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
cargo install cargo-binstall

# Install Ruby


# Install Node
curl -fsSL https://fnm.vercel.app/install | bash
fnm install v24
# pnpm
curl -fsSL https://get.pnpm.io/install.sh | sh -


# Install Py
curl -fsSL https://pyenv.run | bash



# Install my stuff
# clone private_dotfiles
cd $HOME && git clone git@gihub.com:/thenotary/.full_context.git

git clone https://github.com/thenotary/dotfiles.git && \
  cd dotfiles && \
  ruby make.rb

gem install foobar_templates
foobar_templates --install-public-templates
foobar_templates --setup-personal-templates


# Private dotfiles (includes ssh configs)


